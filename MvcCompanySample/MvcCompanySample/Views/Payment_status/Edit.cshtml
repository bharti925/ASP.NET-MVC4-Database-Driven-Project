@model MvcCompanySample.Models.Payment_status

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Payment_status</legend>

        @Html.HiddenFor(model => model.Payment_statusId)

        <div class="editor-label">
            @Html.LabelFor(model => model.Payment_status_name)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Payment_status_name)
            @Html.ValidationMessageFor(model => model.Payment_status_name)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
